# 한국은행 2025 컴퓨터공학 학술 문제

> 신입직원(종합기획직원 G5) 채용고시  
> 시행일: 2024. 9. 28. (토)  
> 과목: 학술(컴퓨터공학)

## 유의사항

1. 성명 및 접수번호는 매 페이지마다 기재하시기 바랍니다.
2. 문제지(또는 답안지)를 낱장으로 뜯어서 사용하는 경우에도 최종 제출시 페이지 번호 순으로 정렬되었는지 확인하시기 바랍니다.
3. 필요시 답안을 영어로 작성할 수 있습니다.
4. 본 문제지는 1교시 전공학술 문제지이며, 2교시 논술(공통) 문제지는 별도로 제공됩니다.

> 동 기출문제는 실제 출제되었던 문제 중 각 유형별 일부 문항을 발췌한 것임

---

# I. 다음 물음에 답하시오.

## 1. 데이터베이스

아래는 학생의 수강 및 지도 현황을 나타내는 **수강지도 릴레이션**이다. 다음 물음에 답하시오.

| 학번 | 지도교수 | 학과 | 과목번호 | 성적 | 학년 |
|---:|---|---|---|---|---:|
| 100 | P1 | 컴퓨터 | C413 | A | 4 |
| 100 | P1 | 컴퓨터 | E412 | A | 4 |
| 200 | P2 | 경제 | C123 | B | 3 |
| 300 | P3 | 경영 | C312 | A | 1 |
| 300 | P3 | 경영 | C324 | C | 1 |
| 300 | P3 | 경영 | C413 | A | 1 |
| 400 | P1 | 컴퓨터 | C312 | A | 4 |
| 400 | P1 | 컴퓨터 | C324 | A | 4 |
| 400 | P1 | 컴퓨터 | C413 | B | 4 |
| 400 | P1 | 컴퓨터 | E412 | C | 4 |

주) 밑줄 친 부분은 기본키(Primary Key)를 의미

### 가.

릴레이션의 데이터 값을 변경하면서 발생하는 이상(Anomaly) 현상에는 **삽입 이상**, **삭제 이상**, **갱신 이상**이 있다. 각 이상 현상에 대해 설명하시오.

### 나.

다음은 수강지도 릴레이션의 함수 종속 다이어그램(Functional Dependency Diagram)이다. 이 다이어그램을 바탕으로 수강지도 릴레이션의 부분적 함수 종속을 제거하는 **제2정규화**를 수행하여 두 개의 함수 종속 다이어그램으로 표현하시오.  
단, 각 릴레이션에 대한 기본키에는 밑줄을 표시할 것.

```text
함수 종속 다이어그램

[성적]  ←  [학번, 과목번호]
             │
             ├── [지도교수]
             │       │
             │       └── [학과]
             └── [학과]

기본키: (학번, 과목번호)
```

### 다.

문제 `나`에서 제2정규화를 수행하였음에도 이행적 함수 종속으로 문제가 발생할 수 있다. **이행적 함수 종속**이 무엇인지 설명하고, 이를 해결하기 위해 **제3정규화**를 수행하여 두 개의 릴레이션으로 작성하시오.  
단, 각 릴레이션에 대한 기본키에는 밑줄을 표시할 것.

### 라.

정규화(Normalization)의 장점과 단점을 서술하시오.

---

## 2. 프로그래밍 언어: 객체지향 개념

프로그래밍 언어와 관련하여 다음 물음에 답하시오.

### 가.

캡슐화(Encapsulation), 상속(Inheritance), 다형성(Polymorphism)에 대해 서술하시오.

### 나.

오버로딩(Overloading), 오버라이딩(Overriding)에 대해 서술하시오.

### 다.

클래스와 객체에 대해 서술하시오.

---

## 3. 프로그래밍 언어: Java 코드 실행 결과

프로그래밍 언어와 관련하여 다음 물음에 답하시오.

### 가.

다음 프로그램의 실행 결과와 그 이유를 답하시오.

```java
public static void main(String[] args){
    String str1 = new String("한국은행");
    String str2 = new String("한국은행");

    if(str1 == str2)
        System.out.println("일치");
    else
        System.out.println("불일치");
}
```

### 나.

다음 프로그램의 실행 결과를 표 우측 빈 공간에 작성하시오.

```java
class Parent{
    Parent( ){
        System.out.println("Parent 1");
    }
    Parent(String str){
        System.out.println("Parent 2");
    }
}

class Child1 extends Parent{
    Child1( ){
        System.out.println("Child 1");
    }
}

class Child2 extends Parent{
    Child2(String str){
        System.out.println("Child 2");
    }
}

class FamilyTest{
    public static void main(String[] args){
        Child1 c1 = new Child1( );
        Child2 c2 = new Child2("a");
    }
}
```

```text
<실행 결과>


```

---

## 4. 소프트웨어 공학

소프트웨어 공학과 관련하여 다음 물음에 답하시오.

### 가.

폭포수(waterfall) 모델과 애자일(agile) 모델에 대해 서술하시오.

### 나.

생성 패턴 중 싱글톤(singleton) 패턴에 대해 서술하시오.

### 다.

알파 테스트와 베타 테스트에 대해 서술하시오.

---

## 5. 데이터 통신

데이터 통신과 관련하여 다음 물음에 답하시오.

### 가.

다음 표는 OSI 7 계층과 TCP/IP에 대한 구조를 나타내고 있다. 표에 나와 있는 각 계층에 다음 10개의 통신 프로토콜들을 채우시오.

```text
DNS, POP3, ICMP, IP, ARP, Ethernet, TCP, HTTP, FTP, UDP
```

| OSI 7 계층 | TCP/IP | 프로토콜(10개) |
|---|---|---|
| 어플리케이션 | 어플리케이션 |  |
| 프리젠테이션 | 어플리케이션 |  |
| 세션 | 어플리케이션 |  |
| 트랜스포트 | 트랜스포트 |  |
| 네트워크 | 네트워크 |  |
| 데이터링크 | 데이터링크 |  |
| 물리 | 물리 |  |

### 나.

TCP와 UDP 프로토콜에 대해 서술하시오.

### 다.

TCP의 3-way handshake에 대해 서술하시오.

---

## 6. DNS

DNS(Domain Name System)는 계층 구조로 분산된 서버들로 구성되어 있고 각 서버에는 자원 레코드(RR, Resource Record)를 저장하고 있다. 각 자원 레코드는 아래와 같이 4개의 필드로 구성된 튜플(Tuple)로 구성되어 있으며 이 정보를 통해 사용자는 원하는 IP 및 기타 정보를 획득할 수 있다.

```text
(Name, Value, Type, TTL)
```

### 가.

DNS 레코드 Type 종류를 3가지 이상 간략히 설명하시오.  
예: AAAA 레코드: 도메인 정보와 IPv6 주소를 저장

### 나.

다음의 그림에서 DNS의 **재귀적 질의(Recursive Query)**와 **반복적 질의(Iterative Query)**의 흐름도를 작성하시오.  
요청 호스트로부터 시작하며 처음 1번과 마지막 8번은 표시되어 있음.

```text
재귀적 질의(Recursive Query)

요청 호스트 → Local DNS 서버: ①
Local DNS 서버 ↔ Root DNS 서버
Root DNS 서버 ↔ Top Level Domain DNS 서버
Top Level Domain DNS 서버 ↔ Authoritative DNS 서버
Local DNS 서버 → 요청 호스트: ⑧

반복적 질의(Iterative Query)

요청 호스트 → Local DNS 서버: ①
Local DNS 서버 ↔ Root DNS 서버
Local DNS 서버 ↔ Top Level Domain DNS 서버
Local DNS 서버 ↔ Authoritative DNS 서버
Local DNS 서버 → 요청 호스트: ⑧
```

---

## 7. 네트워크 보안

네트워크 보안과 관련하여 다음의 물음에 답하시오.

### 가.

HTTP와 HTTPS의 차이점을 설명하시오.

### 나.

가상사설망(VPN, Virtual Private Network)과 IPSec에 대해 설명하시오.

### 다.

DoS(Denial-of-Service)와 DDoS(Distributed Denial-of-Service) 공격에 대해 설명하시오.

---

## 8. 컴퓨터구조: RAID

컴퓨터구조와 관련하여 다음 물음에 답하시오.

### 가.

다음은 여러 물리 디스크를 마치 하나의 논리 장치처럼 사용하는 RAID 기술의 일부를 나타낸 그림이다. **A 방식**과 **B 방식**은 각각 어떤 RAID 단계(RAID-n)인지 쓰고, A 방식 대비 B 방식의 장점을 서술하시오.

#### A 방식

| Disk1 | Disk2 | Disk3 | Disk4 |
|---|---|---|---|
| A1 | A2 | A3 | parity A |
| B1 | B2 | parity B | B3 |
| C1 | parity C | C2 | C3 |
| parity D | D1 | D2 | D3 |

#### B 방식

| Disk1 | Disk2 | Disk3 | Disk4 | Disk5 |
|---|---|---|---|---|
| A1 | A2 | A3 | parity 1 A | parity 2 A |
| B1 | B2 | parity 1 B | parity 2 B | B3 |
| C1 | parity 1 C | parity 2 C | C2 | C3 |
| parity 1 D | parity 2 D | D1 | D2 | D3 |

---

## 9. 컴퓨터구조: 캐시 메모리

컴퓨터구조의 캐시 메모리(cache memory)와 관련하여 다음 물음에 답하시오.

### 가.

캐시 메모리를 사용하면서 캐시의 내용이 변경되었을 때, 그 내용을 주기억장치에 갱신하는 시기와 방법을 결정하는 것을 쓰기 정책(write policy)이라고 한다. 다음 그림의 쓰기 정책에 대해 각각 정의와 장·단점을 서술하시오.

| write-through | write-back |
|---|---|
| CPU가 캐시와 주기억장치에 동시에 기록하는 방식 | CPU가 우선 캐시에 기록하고, 이후 필요한 시점에 주기억장치에 반영하는 방식 |

### 나.

주기억장치 접근 시간이 100ns, 캐시 접근 시간이 10ns인 시스템이 있다. 전체 접근 요구 중에서 80%는 읽기 동작이고, 20%는 쓰기 동작이었으며, 캐시 히트율은 90%였다. 캐시 쓰기 정책이 **write-through**일 때 읽기 동작 시간, 쓰기 동작 시간, 평균 접근 시간을 각각 구하시오.  
읽기와 쓰기 동작에 별도의 오버헤드는 없다고 가정한다.

```text
· 읽기 동작 시간:
· 쓰기 동작 시간:
· 평균 접근 시간:
```

---

## 10. 자료구조: 해시

자료구조와 관련하여 다음 물음에 답하시오.

### 가.

해시 함수에서 충돌이 발생하는 이유와 해결 방법인 **개방 주소법(Open Addressing)**과 **체이닝(Chaining)**에 대해 서술하시오.

### 나.

아래 좌측은 해시 테이블에 데이터를 삽입하기 위한 프로그램이며, 우측의 해시 테이블은 `insertHash` 함수를 사용하여 18, 37, 25를 삽입한 결과이다. 22, 35, 50, 63을 순차적으로 `insertHash` 함수를 사용하여 삽입했을 때의 결과를 우측의 해시 테이블에 작성하고, 이 방법의 문제점에 대해 서술하시오.

```c
#define SIZE 13

int hashTable[SIZE] = {};

int Hash(int key){
    return key % SIZE;
}

void insertHash(int key){
    int probe = Hash(key);
    while(hashTable[probe] != NULL){
        probe = (probe + 1) % SIZE;
    }
    hashTable[probe] = key;
}
```

초기 해시 테이블:

| index | value |
|---:|---:|
| 0 |  |
| 1 |  |
| 2 |  |
| 3 |  |
| 4 |  |
| 5 | 18 |
| 6 |  |
| 7 |  |
| 8 |  |
| 9 |  |
| 10 |  |
| 11 | 37 |
| 12 | 25 |

---

## 11. 운영체제: 교착상태와 스케줄링

운영체제와 관련하여 다음 물음에 답하시오.

### 가.

교착상태를 회피(Avoidance)하기 위한 알고리즘에는 은행원 알고리즘(Banker's Algorithm)이 있다. 은행원 알고리즘은 자원 할당 결정 전에 예상되는 모든 자원의 최대 할당량을 가지고 시뮬레이션하여 안전한 상태(Safe State)에서만 자원 요청을 허용하는 방법이다. 아래의 표를 보고 은행원 알고리즘을 이용하여 가용자원을 어떤 순서로 할당할 수 있는지 쓰시오.  
단, 시스템은 총 12개의 자원을 가지고 있다고 가정.

| 프로세스 | 할당 중인 자원량 | 추가로 필요한 자원량 |
|---|---:|---:|
| P0 | 5 | 5 |
| P1 | 2 | 2 |
| P2 | 2 | 7 |

### 나.

프로세스 스케줄링의 유형에는 **선점형 스케줄링(Preemptive Scheduling)**과 **비선점형 스케줄링(Non-Preemptive Scheduling)**이 있다. 두 스케줄링에 대해 설명하고 장·단점을 각각 서술하시오.

### 다.

프로세스 스케줄링 알고리즘에는 ① FCFS(First-Come-First-Served) 스케줄링, ② SRT(Shortest Remaining Time First) 스케줄링이 있다. 단일 프로세서 환경에서 아래와 같이 프로세스가 도착했을 때, 위의 두 프로세스 스케줄링 알고리즘에 따른 프로세스의 실행 순서를 간트 차트(Gantt Chart)에 표현하시오.

| 프로세스 | 도착시간 | 실행시간 |
|---|---:|---:|
| P1 | 0 | 3 |
| P2 | 2 | 6 |
| P3 | 4 | 4 |
| P4 | 8 | 2 |

#### ① FCFS(First-Come-First-Served) 스케줄링

```text

```

#### ② SRT(Shortest Remaining Time First) 스케줄링

```text

```

### 라.

페이징(Paging) 기법 중 **OPT(OPTimal Replacement)**, **LRU(Least Recently Used)**, **LFU(Least Frequently Used)** 기법에 대해 설명하시오.

---

## 12. 정보보호: ARP 공격

정보보호와 관련하여 다음 물음에 답하시오.

### 가.

ARP(Address Resolution Protocol)는 호스트의 IP주소로부터 하드웨어 주소(MAC, Media Access Control)를 획득하기 위한 통신 프로토콜이다. 만약 특정 PC가 원하는 IP주소에 대한 하드웨어 주소를 알지 못할 때, ARP request를 통하여 로컬 네트워크(LAN, Local Area Network)상의 다른 호스트들에게 질의한 후, 전달받은 ARP response를 통해 ARP 캐시 테이블을 갱신한다. 하지만 ARP는 ARP response를 받아 테이블을 갱신할 때 호스트에 대한 인증과정이 없기에 보안에 취약한 것으로 알려져 있다.

아래 로컬 네트워크(LAN)에 사이버 공격이 발생하고 있다고 가정할 때, 아래 그림의 ARP 캐시 테이블 정보를 바탕으로 **PC A, B, C 중 공격자 호스트 PC를 판별**하고 현재 네트워크 상에서 이루어지고 있는 사이버 공격과 그 공격의 원리를 설명하시오.  
`192.168.1.254`는 게이트웨이 라우터의 IP.

#### 네트워크 구성

```text
PC A: 192.168.1.1, MAC: A
PC B: 192.168.1.2, MAC: B
PC C: 192.168.1.3, MAC: C
라우터(게이트웨이): 192.168.1.254, MAC: X
```

#### ARP 캐시 테이블

| 호스트 | IP | MAC |
|---|---|---|
| PC A의 캐시 | 192.168.1.2 | B |
| PC A의 캐시 | 192.168.1.3 | C |
| PC A의 캐시 | 192.168.1.254 | B |
| PC B의 캐시 | 192.168.1.1 | A |
| PC B의 캐시 | 192.168.1.3 | C |
| PC B의 캐시 | 192.168.1.254 | X |
| PC C의 캐시 | 192.168.1.1 | A |
| PC C의 캐시 | 192.168.1.2 | B |
| PC C의 캐시 | 192.168.1.254 | B |

### 나.

위 공격이 성공한 후에도 공격자가 지속하여 은닉할 수 있었다. 그 이유를 설명하시오.

### 다.

위 공격에 대한 기술적 보안대책을 설명하시오.

---

## 13. 정보보호: 사회공학, XSS, CSRF

정보보호와 관련하여 다음 물음에 답하시오.

### 가.

사회공학 기법(Social Engineering)에 대하여 설명하고 관련된 예시를 서술하시오.

### 나.

웹 어플리케이션 공격 중 **사이트 간 스크립팅(XSS, Cross Site Scripting)** 및 **사이트 간 요청 위조(CSRF, Cross Site Request Forgery)**에 대하여 각각 설명하고 차이점을 비교하시오.

---

## 14. 머신러닝

머신러닝과 관련하여 다음 물음에 답하시오.

### 가.

인공신경망(ANN, Artificial Neural Network)을 학습시키기 위한 **역전파(Backpropagation)** 알고리즘에 대하여 설명하시오.

### 나.

아래와 같은 순환신경망(RNN, Recurrent Neural Network)을 학습하는 과정에서 발생할 수 있는 **기울기 소실(Vanishing Gradients)** 문제를 정의하고 발생하는 이유를 서술하시오.  
동 순환신경망은 활성화 함수(Activation Function)로 시그모이드(Sigmoid) 함수를 사용한다고 가정.

```text
        Output         Output         Output
        y_{t-1}        y_t            y_{t+1}
           ↑             ↑              ↑
           V             V              V
... → W → RNN(h_{t-1}) → W → RNN(h_t) → W → RNN(h_{t+1}) → W → ...
           ↑             ↑              ↑
           U             U              U
        x_{t-1}        x_t            x_{t+1}
        Input          Input          Input
```

---

## 15. 인공지능

인공지능과 관련하여 다음 물음에 답하시오.

### 가.

대규모 언어모형과 관련하여 **사전학습(Pre-training)** 및 **미세조정(Fine-tuning)**에 대하여 설명하시오.

---

